# Copy this file to .env and fill in real values. .env is gitignored —
# never commit real keys.

ANTHROPIC_API_KEY=

# Optional. Overrides the Ollama model __main__.py uses for the local
# fallback backend. Only matters once Ollama is actually installed.
# CHAO_OLLAMA_MODEL=llama3.1:8b-instruct-q4_K_M
